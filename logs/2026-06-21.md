# 开发日志 - 2026-06-21

## 已完成事项

| 模块 | 事项 | 说明 |
|---|---|---|
| 后端启动 | MySQL 连接修复 | 在 JDBC URL 中补充 `allowPublicKeyRetrieval=true`，解决 MySQL 8 本地连接认证失败 |

## 今日问题与解决

| 问题 | 原因 | 解决 |
|---|---|---|
| 后端启动失败 | MySQL 8 使用 `caching_sha2_password` 认证时，JDBC 默认不允许获取公钥，报错 `Public Key Retrieval is not allowed` | 修改 `backend/src/main/resources/application.yml` 的数据库连接串，补充 `allowPublicKeyRetrieval=true` |

## 验收结果

| 命令 | 结果 |
|---|---|
| `cd backend && mvn.cmd spring-boot:run` | 通过，后端成功连接 MySQL，并启动到 `http://localhost:8080`；验证后已停止进程 |

## 待办事项

| 优先级 | 事项 |
|---|---|
| 中 | 后续可考虑将本地数据库账号密码改为环境变量，避免配置文件暴露个人密码 |
